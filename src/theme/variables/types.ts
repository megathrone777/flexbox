export interface TColors {
  blue: string;
  blueDarker: string;
  green: string;
  greenDarker: string;
}

export interface TFonts {
  bold: number;
  initialFontSize: number;
  medium: number;
  normal: number;
}

export interface TDevices {
  mobile: string;
  tablet: string;
}
