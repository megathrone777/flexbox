import { styled } from "~/theme";

export const StyledWrapper = styled.div({
  alignItems: "stretch",
  columnGap: 20,
  display: "flex",
  height: "100%",
  padding: 20,
});

export const StyledContent = styled.div({
  flexGrow: 1,
});
